<x-layout>

    <x-slot:btn>
        <a href="{{route('home.page')}}" class="btn btn-primary">
            Home
        </a>
        <a href="{{route('auth.login')}}" class="btn btn-primary">
            Entre
        </a>
    </x-slot:btn>

    register
</x-layout>
